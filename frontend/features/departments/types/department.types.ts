/**
 * Re-exports Department types from the service layer.
 * Keeps feature/types path consistent with other modules.
 */
export type { Department, DepartmentHead, DepartmentDesignation, CreateDepartmentDto, UpdateDepartmentDto } from '../../../services/api/department.service';
