import { PortalShell } from '../../../features/candidates/components/portal/PortalShell';

export default function PortalShellLayout({ children }: { children: React.ReactNode }) {
  return <PortalShell>{children}</PortalShell>;
}
